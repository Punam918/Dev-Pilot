kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# Use an explicitly installed policy-enforcing CNI, not kind's default kindnet.
networking:
  disableDefaultCNI: true
  podSubnet: 192.168.0.0/16
nodes:
  - role: control-plane
